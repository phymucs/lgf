from setuptools import setup, find_packages

setup(
    name='lgf',
    author='Anonymous',
    version='0.0.1',
    description='Code for Localised Generative Flows paper',
    packages=find_packages()
)
